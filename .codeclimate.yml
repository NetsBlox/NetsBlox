version: "2" # required to adjust maintainability checks
plugins:
  csslint:
    enabled: true
  eslint:
    enabled: true
  fixme:
    enabled: true
  duplication:
    enabled: true
    config:
      languages:
        javascript:
          mass_threshold: 140
exclude_paths:
- test/
- src/client/Snap--Build-Your-Own-Blocks/
